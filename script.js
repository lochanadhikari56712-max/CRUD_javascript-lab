let editIndex = [null];
function addStudent() {
  let name = document.getElementById("nameInput").value;

  if (name.trim() === "") {
    alert("Please enter a name");
    return;
  }
}
if(editIndex === null){

let student = {
id: students.length + 1,
name: name
}